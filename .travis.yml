sudo: required
services:
 - docker

before_install:
 - docker build -t shavitnetzer/docker-react .

script:
 - docker run --name docker-react -p 8081:8081 -d shavitnetzer/docker-react 
 - docker ps
 - netstat -nptul
 - curl -q localhost:8081
 - docker rm -f docker-react
